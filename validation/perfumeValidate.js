const { check } = require('express-validator/check');

module.exports = perfumeValidate = [
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('handle', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('description', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('twoPrc', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('fivePrc', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('tenPrc', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('twoLeft', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('fiveLeft', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('tenLeft', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty(),
  check('name', '此处为必填区域。')
    .not()
    .isEmpty()
];
